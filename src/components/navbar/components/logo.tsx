// Dependencies
import { FC } from "react";
import { motion } from "framer-motion";

const Logo: FC = () => {
  return (
    <motion.a
      href="#"
      className="grid h-16 w-16 place-content-center bg-transparent "
    >
   zahw
    </motion.a>
  );
};

export default Logo;
